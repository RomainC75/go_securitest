version: "1"
packages:
  - name: "db"
    path: "./sqlc"
    schema: "./migration"
    queries: "./query"
    engine: "postgresql"
    emit_db_tags: false
    emit_prepared_queries: false
    emit_interface: true
    emit_exact_table_names: false
    # instead of nil
    emit_empty_slices: true
